import { AppShell } from '@/components/shell/AppShell';

export default function OportunidadesLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="Oportunidades">{children}</AppShell>;
}
