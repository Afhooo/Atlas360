import { AppShell } from '@/components/shell/AppShell';

export default function ProductividadLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="Productividad">{children}</AppShell>;
}
