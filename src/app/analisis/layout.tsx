import { AppShell } from '@/components/shell/AppShell';

export default function AnalisisLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="Análisis con IA">{children}</AppShell>;
}

