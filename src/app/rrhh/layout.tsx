import { AppShell } from '@/components/shell/AppShell';

export default function RrhhLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="RRHH y Asistencia">{children}</AppShell>;
}
